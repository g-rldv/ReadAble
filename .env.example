# ── Backend ──────────────────────────────────────────────────
NODE_ENV=development
PORT=5000

# PostgreSQL connection string
DATABASE_URL=postgresql://readable_user:password@localhost:5432/readable

# JWT secret key (use a long random string in production)
JWT_SECRET=change-this-to-a-long-random-secret-in-production

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# ── Frontend ──────────────────────────────────────────────────
VITE_API_URL=http://localhost:5000

# ── Email (Resend) ────────────────────────────────────────────
# Get your API key at https://resend.com/api-keys
RESEND_API_KEY=re_XSeXjB44_Jub86q5pziYyVuPsKrWDCsfY

# Must be a verified sender in your Resend account
# For local testing you can use: onboarding@resend.dev
SMTP_FROM="ReadAble" <onboarding@resend.dev>
